name: PR smoke tests workflow call test

on: 
  pull_request:

permissions:
    contents: write
    pull-requests: write

jobs:
    call_smoke_tests_workflow:
        uses: rafark/cicd-labs/.github/workflows/build.yml@main
